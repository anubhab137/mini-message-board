extends layout

block content
  h1= title
  br
  br
  br
  ul
    each message in messages
      h4 #{message.user}: 
      p #{message.text}
      p <small>#{message.added}</small> 
  br
  br
  a(class='button',href='/new') Reply    
  
